apply plugin : 'camerakit-library'

android {
    externalNativeBuild {
        cmake {
            path 'src/main/cpp/CMakeLists.txt'
        }
    }
}

camerakit {
    artifact = 'core'
    name = 'camerakit'
    description = ''
    publish = true
}
